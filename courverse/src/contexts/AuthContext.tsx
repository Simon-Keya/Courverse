/**
 * @deprecated Use useAuthStore from @/store/auth.store and AuthProvider from @/providers/AuthProvider.
 * Kept as a thin re-export so accidental imports do not break the build.
 */
export { useAuthStore as useAuth } from "@/store/auth.store";
